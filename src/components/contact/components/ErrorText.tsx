import { styled } from '@mui/material/styles'

const ErrorText = styled('div')(() => {
  return {
    color: '#dc3545',
    fontSize: '.875rem',
    marginTop: '.25rem'
  }
})

export default ErrorText
